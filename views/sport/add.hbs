<!DOCTYPE html>
<html lang="en">

<head>
   <meta charset="UTF-8">
   <meta name="viewport" content="width=device-width, initial-scale=1.0">
   <title>Add New Sport</title>
   <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
   <style>
       /* Custom CSS Styles */
       body {
           background-color: #f0f2f5; /* Light gray background for the whole page */
       }

       .container {
           margin-top: 50px; /* Add top margin to move the form down */
       }

       .card {
           border-radius: 10px; /* Rounded corners for the card */
           box-shadow: 0 4px 8px rgba(0,0,0,0.2); /* Shadow effect for the card */
       }

       .card-header {
           background-color: #007bff; /* Primary color for the header */
           color: white; /* White text color */
           border-bottom: 2px solid #0056b3; /* Darker border at the bottom */
           padding: 15px; /* Padding inside the header */
       }

       .card-body {
           padding: 30px; /* Increase padding inside the card body */
       }

       .form-group label {
           font-size: 1.1rem; /* Slightly larger font size for labels */
           color: #dc3545; /* Danger color for labels */
       }

       .form-control {
           border-radius: 5px; /* Rounded corners for input fields */
           box-shadow: inset 0 1px 2px rgba(0,0,0,0.1); /* Inner shadow effect */
       }

       .btn-success {
           width: 100%; /* Make the button full-width */
           border-radius: 5px; /* Rounded corners for the button */
           padding: 10px; /* Increase button padding for better click area */
           font-size: 1.1rem; /* Larger font size for better readability */
           font-weight: bold; /* Bold font weight for the button text */
       }

       .btn-success:hover {
           background-color: #218838; /* Darker green on hover */
           border-color: #1e7e34; /* Darker green border on hover */
       }
   </style>
</head>

<body>
   <div class="container text-center card bg-light col-md-6">
      <form action="/sport/add" method="post">
         <h1 class="alert alert-warning text-uppercase">Add New sport</h1>
         <div class="form-group mt-3">
            <label class="text text-danger fw-bold">Sport Category</label>
            <input class="form-control" type="text" placeholder="Enter sport category" name="category" required minlength="2">
         </div>
         <div class="form-group mt-3">
            <label class="text text-danger fw-bold">Sport Quantity</label>
            <input class="form-control" type="text" placeholder="Enter sport quantity" name="quantity" required minlength="3">
         </div>
         <div class="form-group mt-3">
            <label class="text text-danger fw-bold">Sport Price</label>
            <input class="form-control" type="number" name="Price" placeholder="Enter sport price" required min="0">
         </div>
         <div class="form-group mt-3">
            <label class="text text-danger fw-bold">sport Image URL</label>
            <input class="form-control" type="url" name="image" placeholder="Enter sport image URL">
         </div>
         <div class="form-group mt-3 mb-3">
            <input class="btn btn-success" type="submit" value="Add">
         </div>
      </form>
   </div>

</body>

</html>
